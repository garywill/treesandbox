# heads.py里只能放常量，不能放变量
# 这个文件用 from heads import * 导入 ， 只会被实际执行一次


import os, sys, shutil, subprocess, pwd, grp, time, pty, ctypes, ctypes.util, atexit, json, copy, tempfile, struct, re, socket, signal, asyncio, datetime , types, select, fcntl, traceback, random , errno, shlex, enum, argparse, hashlib, io, resource, string, platform, zipfile
from pathlib import Path
from glob import glob
shutil.rmtree = None
